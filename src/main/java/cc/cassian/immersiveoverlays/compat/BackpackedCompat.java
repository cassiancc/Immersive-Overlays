package cc.cassian.immersiveoverlays.compat;

import net.minecraft.world.entity.player.Player;

public class BackpackedCompat {
	public static void checkForImportantAccessories(Player player) {
//		BackpackedInventoryAccess backpackedInventoryAccess = (BackpackedInventoryAccess) player;
//		BackpackInventory backpackInventory = backpackedInventoryAccess.backpacked$GetBackpackInventory(0);
//		System.out.println("Checking" + backpackInventory);
//		if (backpackInventory == null) return;
//		for(int i = 0; i < backpackInventory.getContainerSize(); ++i) {
//			System.out.println(backpackInventory.getItem(i));
//			OverlayHelpers.isImportantItemOrContainer(backpackInventory.getItem(i));
//		}
	}
}
